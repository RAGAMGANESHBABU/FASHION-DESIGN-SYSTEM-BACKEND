{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" },    // only /api/* goes to backend
    { "src": "/(.*)", "dest": "/$1" }               // static files served normally
  ]
}
